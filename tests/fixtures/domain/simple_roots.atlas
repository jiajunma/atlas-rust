ra : RootDatum
ra := simply_connected(Lie_type("A2"),true)
simple_roots(ra)
simple_coroots(ra)
is_Cartan_matrix([[2,-1],[-1,2]])
is_Cartan_matrix([[1,0],[0,1]])
ic : InnerClass
ic := inner_class(ra,[[1,0],[0,1]])
dual_datum(ic)
rb : RootDatum
rb := simply_connected(Lie_type("B2"),true)
simple_roots(rb)
simple_coroots(rb)
rg : RootDatum
rg := simply_connected(Lie_type("G2"),true)
simple_roots(rg)
simple_coroots(rg)
rf : RootDatum
rf := simply_connected(Lie_type("F4"),true)
simple_roots(rf)
is_Cartan_matrix([[2,-1,0,0],[-1,2,-2,0],[0,-1,2,-1],[0,0,-1,2]])
rd : RootDatum
rd := simply_connected(Lie_type("D4"),true)
simple_roots(rd)
simple_coroots(rd)
re : RootDatum
re := simply_connected(Lie_type("E7"),true)
simple_roots(re)
simple_coroots(re)
re8 : RootDatum
re8 := simply_connected(Lie_type("E8"),true)
simple_roots(re8)
simple_coroots(re8)
re : RootDatum
re := simply_connected(Lie_type("E6"),true)
simple_roots(re)
simple_coroots(re)
rc : RootDatum
rc := simply_connected(Lie_type("C3"),true)
simple_roots(rc)
simple_coroots(rc)
rb : RootDatum
rb := simply_connected(Lie_type("B3"),true)
simple_roots(rb)
simple_coroots(rb)
ra1 : RootDatum
ra1 := simply_connected(Lie_type("A1"),true)
simple_roots(ra1)
simple_coroots(ra1)
ra4 : RootDatum
ra4 := simply_connected(Lie_type("A4"),true)
simple_roots(ra4)
simple_coroots(ra4)
re7 : RootDatum
re7 := simply_connected(Lie_type("E7"),true)
simple_roots(re7)
simple_coroots(re7)
re8 : RootDatum
re8 := simply_connected(Lie_type("E8"),true)
simple_roots(re8)
simple_coroots(re8)
