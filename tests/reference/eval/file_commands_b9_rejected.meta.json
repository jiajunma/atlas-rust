{
  "fixture": "eval/file_commands_b9_rejected",
  "oracle": "atlas",
  "stage": "typed-pipeline-swap",
  "reference_status": "pending_hpc_reference",
  "upstream_evidence": [
    "sources/interpreter/lexer.w:558-560",
    "sources/interpreter/parser.y:180-181",
    "sources/interpreter/main.w:504-511"
  ],
  "rust_status": "not_implemented",
  "notes": "Rejected companions of eval/file_commands_b9, split from probe file_commands_probe (capture 3499729). A tofile command accepts only an expression: '> \"x\" set qfc = 10' is a bison syntax error whose offending token is '=' (the oracle's full line reads \"syntax error, unexpected '=', expecting :=\"; per convention only the offending token is asserted, and the oracle's caret echo line is not part of the contract). Failing to open the redirect target prints 'Failed to open <name>' directly on stderr (not via the error machinery), skips the line, and batch evaluation continues, so it is classified as a runtime diagnostic. Reference capture pending on HPC."
}
