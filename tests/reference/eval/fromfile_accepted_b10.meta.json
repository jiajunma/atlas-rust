{
  "fixture": "eval/fromfile_accepted_b10",
  "oracle": "atlas",
  "stage": "typed-pipeline-swap",
  "reference_status": "pending_hpc_reference",
  "upstream_evidence": [
    "sources/interpreter/lexer.w:558-560",
    "sources/interpreter/parser.y:182-183",
    "sources/interpreter/parsetree.w:3193-3197",
    "sources/interpreter/buffer.w:770-786"
  ],
  "rust_status": "not_implemented",
  "notes": "B10 accepted fromfile inclusion, content-identical split from probe fromfile_accepted_probe (capture 3500381). Inclusion reports 'Starting to read from file '<path>'.' and 'Completely read file '<path>'.' bracketing the included file's own output indented by two spaces; definitions persist after inclusion. A plain '<' include of an already-completed file is a SILENT skip (buffer.w:770-776 push_file with skip_seen), so fixture line 3 produces no event; '<<' force re-includes, and the re-definition inside reports 'Variable xb10: int (overriding previous instance, which had type int)'. The helper is referenced by absolute HPC path so oracle (cwd atlas-scripts) and Rust CLI (cwd workspace root) resolve identically; local runs need the same layout. Reference capture pending on HPC."
}
